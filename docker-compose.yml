services:
  telegram-bot:
    build:
      context: ./bot
      dockerfile: Dockerfile
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      GOOGLE_SHEETS_ID: ${GOOGLE_SHEETS_ID:-}
      GOOGLE_SERVICE_ACCOUNT_JSON: ${GOOGLE_SERVICE_ACCOUNT_JSON:-}
      GOOGLE_SHEET_NAME: ${GOOGLE_SHEET_NAME:-Leads}
      LOCAL_TIMEZONE: ${LOCAL_TIMEZONE:-UTC}
      POLL_INTERVAL_SECONDS: ${POLL_INTERVAL_SECONDS:-1}
      MAX_RETRY_ATTEMPTS: ${MAX_RETRY_ATTEMPTS:-3}
      RETRY_DELAY_SECONDS: ${RETRY_DELAY_SECONDS:-1}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    command: ["python", "bot.py"]
    restart: unless-stopped
    networks:
      - lead-network

  telegram-bot-test:
    build:
      context: ./bot
      dockerfile: Dockerfile
    command: ["python", "-m", "pytest", "tests", "-q"]
    profiles: ["test"]
    networks:
      - lead-network

  web-assistant:
    build:
      context: ./web_assistant
      dockerfile: Dockerfile
    environment:
      WEB_ASSISTANT_HOST: ${WEB_ASSISTANT_HOST:-0.0.0.0}
      WEB_ASSISTANT_PORT: ${WEB_ASSISTANT_PORT:-5000}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      GOOGLE_SHEETS_ID: ${GOOGLE_SHEETS_ID:-}
      GOOGLE_SHEET_NAME: ${GOOGLE_SHEET_NAME:-Leads}
      GOOGLE_SERVICE_ACCOUNT_JSON: ${GOOGLE_SERVICE_ACCOUNT_JSON:-}
      MAX_RETRY_ATTEMPTS: ${MAX_RETRY_ATTEMPTS:-3}
      RETRY_DELAY_SECONDS: ${RETRY_DELAY_SECONDS:-1}
      LOCAL_TIMEZONE: ${LOCAL_TIMEZONE:-UTC}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    command: ["python", "app.py"]
    expose:
      - "5000"
    restart: unless-stopped
    networks:
      - lead-network

  web-assistant-test:
    build:
      context: ./web_assistant
      dockerfile: Dockerfile
    command: ["python", "-m", "pytest", "tests", "-q"]
    profiles: ["test"]
    networks:
      - lead-network

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web-assistant
    restart: unless-stopped
    networks:
      - lead-network

networks:
  lead-network:
    driver: bridge
